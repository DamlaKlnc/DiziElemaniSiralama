import java.util.Arrays;
import java.util.Scanner;

public class HelpArr {
    static void getUp(int []arr){
        Arrays.sort(arr);
        System.out.print("Sıralama : "+Arrays.toString(arr));
    }
}
